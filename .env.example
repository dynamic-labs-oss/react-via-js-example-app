# Copy to `.env` and set your Environment ID from the Dynamic dashboard.
VITE_DYNAMIC_ENVIRONMENT_ID=

# Optional overrides (defaults match production Dynamic API)
# VITE_DYNAMIC_API_BASE_URL_ORIGIN=https://app.dynamic.xyz/
# VITE_DYNAMIC_API_BASE_URL_PATH=api/v0/
